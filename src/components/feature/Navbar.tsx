// Navbar is now rendered as static HTML directly in each Astro page's <head>/<body>.
// This stub keeps all existing react-page imports working without rendering anything —
// the real navbar HTML is injected by the Astro layout/page wrapper.
export default function Navbar() {
  return null;
}
